document.addEventListener('DOMContentLoaded',()=>{Navigation.init();EasterEggs.init();VideoSystem.init();const m=document.createElement('button');m.className='music';m.type='button';m.textContent='MUSIC ON';document.body.append(m);m.onclick=()=>{if(AudioSystem.music&&!AudioSystem.music.paused){AudioSystem.music.pause();m.textContent='MUSIC ON'}else{AudioSystem.start();m.textContent='MUSIC OFF'}};const controls=document.createElement('div');controls.className='mobile-controls';controls.innerHTML='<button class="scene-next" type="button" aria-label="Đi đến trang tiếp theo">TIẾP <span aria-hidden="true">→</span></button>';document.body.append(controls);const hint=document.createElement('div');hint.className='interaction-hint';hint.innerHTML='<span class="hint-symbol" aria-hidden="true">↗</span><span><b>VUỐT HOẶC CHẠM</b><small>để tiếp tục câu chuyện</small></span>';document.body.append(hint);document.addEventListener('click',e=>{if(e.target.closest('.scene-next'))Navigation.next()})});