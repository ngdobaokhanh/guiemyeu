const Fireworks={run(){if(!SETTINGS.fireworksEnabled)return;const c=document.getElementById('fireworks'),x=c.getContext('2d');c.width=innerWidth;c.height=innerHeight;const p=[];for(let i=0;i<150;i++){const a=Math.random()*Math.PI*2,s=2+Math.random()*5;p.push({x:c.width/2,y:c.height*.35,vx:Math.cos(a)*s,vy:Math.sin(a)*s,l:80+Math.random()*50})}const f=()=>{x.clearRect(0,0,c.width,c.height);p.forEach(q=>{q.vy+=.04;q.x+=q.vx;q.y+=q.vy;q.l--;x.fillStyle=`hsla(${35+Math.random()*25},75%,70%,${q.l/120})`;x.fillRect(q.x,q.y,2,2)});if(p.some(q=>q.l>0))requestAnimationFrame(f);else x.clearRect(0,0,c.width,c.height)};f()}};
